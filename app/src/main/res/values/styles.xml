<resources>

    <!-- Base application theme. -->
    <style name="RecifyTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
    </style>

    <style name="RecifyTheme.Base.Toolbar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionMenuTextColor">@color/primaryTextColor</item>
    </style>

    <!-- This configures the styles for the title within the Toolbar  -->
    <style name="RecifyTheme.Base.Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">21sp</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- This configures the styles for the title within the Toolbar  -->
    <style name="RecifyTheme.Base.TitleText">
        <item name="android:textSize">40sp</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <!-- This configures the styles for the title within the Toolbar  -->
    <style name="RecifyTheme.Base.SubtitleRecipeDetail">
        <item name="android:textSize">27sp</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <!-- This configures the styles for the title within the Toolbar  -->
    <style name="RecifyTheme.Base.IngredientDetails">
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="RecifyTheme.Materialcomponents" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
    </style>

</resources>
